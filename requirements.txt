requests
pandas
numpy
matplotlib
seaborn
Pillow
tqdm
torch>=2.3        # >=2.3 is numpy-2 compatible; for GPU install the CUDA build (see README)
torchvision>=0.18
scikit-learn
datasets
huggingface_hub
ultralytics       # YOLO subject localizer (nb 06) + FastSAM mask teacher (maintainer script)
diffusers         # seg-controlnet render (nb 12)
transformers      # CLIP style scoring (nb 12)
accelerate        # enable_model_cpu_offload for the low-vram render path (nb 12)
opencv-python     # plan-symbol extraction (nb 14)
# jupyter / jupyterlab assumed already installed; install separately if needed
