# MLflow — local SQLite by default (laptop tier). Set to `databricks` for a shared
# team registry (tier C), after `databricks configure`.
MLFLOW_TRACKING_URI=sqlite:///mlops/mlflow.db

BV_MODEL_NAME=botanical-classifier
BV_MODEL_ALIAS=production
BV_EXPERIMENT=botanical-vision
