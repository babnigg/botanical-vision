# Shared HF model repo for share/publish + leaderboard (default shown)
BV_MODEL_REPO=dbabnigg/botanical-vision-models

# Only needed to publish models or push datasets (or run `huggingface-cli login`)
# HF_TOKEN=hf_...
