# Normalize line endings so Windows and Mac collaborators don't churn diffs
* text=auto eol=lf

# Treat notebooks and data as-is
*.ipynb -text
*.csv text eol=lf

# Model weights live in the MLflow registry, not git — see models/README.md.
