# Backend runtime. The API runs and serves stubs with just these:
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
Pillow>=10

# Model registry + serving-from-registry.
mlflow>=2.14
gunicorn                     # production WSGI/ASGI server for the Docker image
datasets>=2.19               # streaming the HF test split for the evaluation gate

# Real classifier inference. The API detects torch at runtime and falls back to a
# stub if it's absent, but the MLflow pyfunc model needs these to actually run.
torch>=2.3
torchvision>=0.18
